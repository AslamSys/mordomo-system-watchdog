name: Deploy Watchdog

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    # Usa o fluxo central da organizacao AslamSys
    uses: AslamSys/.github/.github/workflows/reusable-deploy.yml@main
    with:
      service_name: "mordomo-system-watchdog"
      hardware_target: "mordomo"
      ecosystem: "infra"
